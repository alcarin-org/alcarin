openapi: 3.0.1
info:
  title: MailTeam Gmail Addon API
  description: Part of the MailTeam system, providing server support for MailTeam Gmail Addon
  version: 1.0.0
servers:
  - url: http://localhost:8080
  - url: http://dev.apollo.codequest.com
paths:
  /test/test:
    summary: Test endpoint
    get:
      tags:
        - test
      responses:
        200:
          description:
            All is fine
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
