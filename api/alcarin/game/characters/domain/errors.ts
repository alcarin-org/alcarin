export function raceNotFound(key: string) {
  return new Error(`race not found for key ${key}`);
}

export function pluginAlreadyRegistered(key: string) {
  return new Error(`race not found for key ${key}`);
}
